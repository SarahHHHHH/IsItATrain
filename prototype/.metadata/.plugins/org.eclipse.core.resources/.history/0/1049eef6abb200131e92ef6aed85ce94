import java.lang.Math.*;

public class SafeControl 
{
	double trainPhysicalSpeedLimit=80.0;
	double tempMax=90;
	double trackSpeedLimit;
	
	
	public SafeControl(double limit)
	{
		trackSpeedLimit=limit;
	}
	
	double fineMin(double min1, double min2)
	{
		if(min1<min2)
			return min1;
		else 
			return min2;
	}
	
	double checkTrainSpeedLimit(double suggetSpeedLimit)
	{
		if(suggetSpeedLimit>fineMin(trainPhysicalSpeedLimit,trackSpeedLimit))
			suggetSpeedLimit=fineMin(trainPhysicalSpeedLimit,trackSpeedLimit);
		return suggetSpeedLimit;
	}
	
	double checkTrainSpeed(double suggestedSpeed)
	{
		if(suggestedSpeed>trainPhysicalSpeedLimit)
			suggestedSpeed=trainPhysicalSpeedLimit;
		return suggestedSpeed;
	}
	
	double checkDoorStatusChange(double currentSpeed, int doorStat)
	{
		if(currentSpeed!=0)
		{
			doorStat=0;
			return doorStat;
		}
		else
		{
			return doorStat;
		}
	}
	
	double checkTrainTemp(double suggestedTemp)
	{
		if(suggestedTemp>tempMax)
			suggestedTemp=tempMax;
		return suggestedTemp;
	}

}
